<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MyShop.Views.Auth.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Auth"
    xmlns:helper="using:MyShop.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LoginWindow" Activated="Window_Activated">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid>
            <Image Source="/Assets/Login/login_background.jpg" Stretch="UniformToFill" />
            <TextBlock Text="My Shop" FontSize="50" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <Grid Grid.Column="1">
            <Grid Grid.Column="1" Background="#fdeee5">
                <StackPanel BorderBrush="Blue" BorderThickness="2" CornerRadius="10" Width="250" Padding="10" Spacing="10" VerticalAlignment="Center" helper:EnterKeyHelper.DefaultButton="{x:Bind loginButton}">
                    <TextBlock Text="Login" Style="{StaticResource TitleTextBlockStyle}"/>
                    <TextBox Name="tb_Username" Header="Username"/>
                    <PasswordBox Name="pb_Password" Header="Password" PasswordChar="*"/>
                    <CheckBox Name="chb_RememberMe" Content="Remember Me" IsChecked="False"/>
                    <StackPanel Orientation="Horizontal" Padding="10" HorizontalAlignment="Center">
                        <Button Name="loginButton" Content="Login" Click="loginButton_Click" Style="{StaticResource AccentButtonStyle}"/>
                        <Button Content="Update" Margin="10 0 0 0"/>
                    </StackPanel>
                    <HyperlinkButton Content="Config" HorizontalAlignment="Center" Margin="0 15 0 5"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
