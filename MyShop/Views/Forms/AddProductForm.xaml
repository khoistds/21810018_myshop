<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="MyShop.Views.Forms.AddProductForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Forms"
    xmlns:converter="using:MyShop.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ContentDialog.Resources>
        <converter:StringToCurrencyConverter x:Key="Currency" Culture="vi-VN"/>
    </ContentDialog.Resources>
    <StackPanel>
        <TextBox Header="Category" Text="{x:Bind NewItem.CategoryId, Mode=TwoWay}"/>
        <TextBox Header="Product Name" Text="{x:Bind NewItem.Name, Mode=TwoWay}" Margin="0,0,0,10"/>
        <TextBox Header="Image" Text="{x:Bind NewItem.ImageUrl, Mode=TwoWay}" Margin="0,0,0,10"/>
        <TextBox Header="Description" Text="{x:Bind NewItem.Description, Mode=TwoWay}" Margin="0,0,0,10"/>
        <TextBox Header="Price" Text="{x:Bind NewItem.Price, Mode=TwoWay}" Margin="0,0,0,10"/>
    </StackPanel>
    
</ContentDialog>
