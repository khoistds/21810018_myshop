<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="productListPage"
    x:Class="MyShop.Views.Products.ProductListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Products"
    xmlns:model="using:MyShop.Models"
    xmlns:converter="using:MyShop.Converters"
    xmlns:helper="using:MyShop.Helpers"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:control="using:MyShop.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--Converter định dạng tiền tệ theo văn hóa Việt Nam-->
        <converter:StringToCurrencyConverter x:Key="Currency" Culture="vi-VN"/>

        <!--Template hiển thị sản phẩm trong ComboBox dạng List-->
        <DataTemplate x:Name="ListTemplate" x:DataType="model:Product">
            <Grid BorderBrush="Blue" BorderThickness="1" CornerRadius="3" Height="Auto" Width="350" Margin="0 5" Padding="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" >
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                <Run Text="{x:Bind Id, Mode=OneWay}" Foreground="Red"/>
                <Run Text="."/>
                    </TextBlock>
                    <Image Source="{x:Bind ImageUrl, Mode=OneWay}" Width="20" Height="20" Margin="5 0"/>
                    <TextBlock HorizontalAlignment="Center">
                <Run Text="{x:Bind Name, Mode=OneWay}" Foreground="Blue"/>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="{x:Bind Price, Mode=OneWay, Converter={StaticResource Currency}}" Foreground="Red" HorizontalTextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <!--Template hiển thị sản phẩm trong ComboBox dạng Thumbnail-->
        <DataTemplate x:Name="ThumbnailTemplate" x:DataType="model:Product">
            <StackPanel Orientation="Vertical" BorderBrush="Blue" BorderThickness="1" CornerRadius="10" Width="250" Height="Auto" Margin="0 10 0 10">
                <TextBlock Text="{x:Bind CategoryId, Mode=OneWay}" HorizontalAlignment="Center" Margin="0 10"/>
                <TextBlock Margin="0 0 0 10" HorizontalAlignment="Center" TextWrapping="Wrap" Height="35">
                    <Run Text="{x:Bind Id, Mode=OneWay}" Foreground="Red"/>
                    <Run Text="."/>
                    <Run Text="{x:Bind Name, Mode=OneWay}" Foreground="Blue"/>
                </TextBlock>
                <Image Source="{x:Bind ImageUrl, Mode=OneWay}" Width="200" Height="200"/>
                <TextBlock Text="{x:Bind Price, Mode=OneWay, Converter={StaticResource Currency}}" Foreground="Red" HorizontalAlignment="Center" Margin="0 10"/>
            </StackPanel>
        </DataTemplate>

    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <InfoBar Grid.Row="0" Grid.Column="0" IsOpen="{x:Bind ViewModel.ShowInfoBar, Mode=TwoWay}" Severity="Success" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="{x:Bind ViewModel.InfoBarMessage, Mode=OneWay}" Foreground="Green"/>
            </StackPanel>
        </InfoBar>

        <StackPanel Grid.Row="1" Grid.Column="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                    
                <Grid Grid.Column="0" Margin="10 0 0 10" Padding="10 0 0 5" BorderBrush="LightGray" BorderThickness="0 0 0 1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Danh sách Sản Phẩm" Style="{StaticResource TitleTextBlockStyle}" HorizontalTextAlignment="Left"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0 0 10 0">
                        <Button Name="addProductButton" Width="Auto" Command="{x:Bind ViewModel.AddNewProductCommand, Mode=OneWay}">
                            <Button.Content>
                                <Image Source="/Assets/Actions/add.png" Width="20" Height="20"/>
                            </Button.Content>
                        </Button>

                        <Button ToolTipService.ToolTip="Thay đổi cách hiển thị">
                            <Button.Content>
                                <Image Source="/Assets/Icons/view.png" Width="20" Height="20"/>
                            </Button.Content>
                            <Button.Flyout>
                                <MenuFlyout Placement="BottomEdgeAlignedRight">
                                    <MenuFlyoutItem Name="listViewButton" Text="Dạng danh sách" Click="listViewButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <ImageIcon Source="/Assets/Icons/list.png"/>
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>

                                    <MenuFlyoutItem Name="thumbnailViewButton" Text="Dạng hình ảnh" Click="thumbnailViewButton_Click">
                                        <MenuFlyoutItem.Icon>
                                            <ImageIcon Source="/Assets/Icons/grid.png"/>
                                        </MenuFlyoutItem.Icon>

                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>

                    </StackPanel>
                </Grid>
                    
                
                
            </Grid>
        </StackPanel>

        <ListView Name="ProductsList" helper:ListViewBehavior.RightClickSelect="True" Grid.Row="2" Grid.Column="0" ItemsSource="{x:Bind ViewModel.Products, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedProduct, Mode=TwoWay}" ItemTemplate="{StaticResource ListTemplate}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <toolkit:WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Add"  Command="{x:Bind ViewModel.AddNewProductCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <ImageIcon Source="/Assets/Actions/add.png"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Edit"  Command="{x:Bind ViewModel.UpdateSelectedProductCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <ImageIcon Source="/Assets/Actions/edit.png"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem Text="Delete"  Command="{x:Bind ViewModel.DeleteSelectedProductCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <ImageIcon Source="/Assets/Actions/delete.png"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </ListView.ContextFlyout>
        </ListView>

        <StackPanel Grid.Row="3" Grid.Column="0">
            <control:PaginationControl Pagination="{x:Bind ViewModel.Pagination , Mode=OneWay}" PageChanged="PaginationControl_PageChanged"/>
        </StackPanel>

        <StackPanel x:Name="ProductInfomationTemplate" x:Load="{x:Bind ViewModel.HasSelection, Mode=OneWay}" Grid.Row="0" Grid.Column="1" Grid.RowSpan="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" BorderBrush="LightGray" BorderThickness="1 0 0 0" Padding="10" Width="300">
            <TextBlock Text="Chi tiết Sản Phẩm" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center"/>
            <TextBlock Text="{x:Bind ViewModel.SelectedProduct.CategoryId, Mode=OneWay}" HorizontalAlignment="Center" Margin="0 10"/>
                
            <TextBlock Margin="0 0 0 10" HorizontalAlignment="Center">
                <Run Text="{x:Bind ViewModel.SelectedProduct.Id, Mode=OneWay}" Foreground="Red"/>
                <Run Text="."/>
                <Run Text="{x:Bind ViewModel.SelectedProduct.Name, Mode=OneWay}" Foreground="Blue"/>
            </TextBlock>
                
            <Image Source="{x:Bind ViewModel.SelectedProduct.ImageUrl, Mode=OneWay}" Width="150" Height="150" Margin="0 10"/>
                
            <TextBlock Text="{x:Bind ViewModel.SelectedProduct.Description, Mode=OneWay}" TextWrapping="Wrap" TextAlignment="Center"/>
                
            <TextBlock Text="{x:Bind ViewModel.SelectedProduct.Price, Mode=OneWay, Converter={StaticResource Currency}}" Foreground="Red" HorizontalAlignment="Center" Margin="0 10"/>
                
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="updateProductButton" Content="Edit" Height="35" Width="70" Margin="10 0 10 0"  Command="{x:Bind ViewModel.UpdateSelectedProductCommand, Mode=OneWay}" Style="{StaticResource AccentButtonStyle}"/>
                <Button Name="deleteProductButton" Content="Delete" Height="35" Width="70"  Command="{x:Bind ViewModel.DeleteSelectedProductCommand, Mode=OneWay}"/>
                <Button Name="closeSelectedProductButton" Content="Close" Height="35" Width="70" Margin="10 0 10 0" Click="closeSelectedProductButton_Click"/>
            </StackPanel>
        </StackPanel>


    </Grid>
    
    
</Page>
