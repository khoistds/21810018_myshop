<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="MyShop.Views.Forms.EditProductForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Forms"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <TextBox Header="Product ID" Text="{x:Bind EditItem.Id, Mode=OneWay}" Margin="0,0,0,10" IsEnabled="False"/>
        <TextBox Header="Category" Text="{x:Bind EditItem.CategoryId, Mode=TwoWay}"/>
        <TextBox Header="Product Name" Text="{x:Bind EditItem.Name, Mode=TwoWay}" Margin="0,0,0,10"/>
        <TextBox Header="Description" Text="{x:Bind EditItem.Description, Mode=TwoWay}" Margin="0,0,0,10"/>
        <NumberBox Header="Price" Text="{x:Bind EditItem.Price, Mode=TwoWay}" Margin="0,0,0,10"/>
    </StackPanel>
</ContentDialog>
